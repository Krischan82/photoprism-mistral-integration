[project]
name = "photoprism-mistral-integration"
version = "0.1.0"
description = "AI-powered photo enrichment for PhotoPrism using Mistral AI vision models."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
